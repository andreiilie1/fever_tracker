from __future__ import annotations

# Temperature threshold (°C) considered critical on the chart and UI.
# Kept in a dedicated module to avoid cross-module magic numbers.
TEMP_CRITICAL: float = 39.8
